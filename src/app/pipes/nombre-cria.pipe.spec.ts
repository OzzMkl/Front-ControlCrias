import { NombreCriaPipe } from './nombre-cria.pipe';

describe('NombreCriaPipe', () => {
  it('create an instance', () => {
    const pipe = new NombreCriaPipe();
    expect(pipe).toBeTruthy();
  });
});
