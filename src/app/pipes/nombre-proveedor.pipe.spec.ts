import { NombreProveedorPipe } from './nombre-proveedor.pipe';

describe('NombreProveedorPipe', () => {
  it('create an instance', () => {
    const pipe = new NombreProveedorPipe();
    expect(pipe).toBeTruthy();
  });
});
